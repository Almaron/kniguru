.innerbox.innerbox_list
  %h2
    = link_to press.paper_name, press
  .content
    = press.head
    %br
    = simple_format "#{press.content.slice(/\A(.{200}.*?)\s/)}[...]"
  .small_date
    = l press.published_on